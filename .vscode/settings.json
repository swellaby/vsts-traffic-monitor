{
    "files.exclude": {
        "**/*.js": { "when": "$(basename).ts" },
        "**/*.map": { "when": "$(basename).map" },
        "npm-debug.log": true,
        ".vsts-publish": true,
        ".coverage": true,
        ".testresults": true,
        "node_modules": false,
        ".taskkey": true
    }
}